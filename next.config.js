/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

// module.exports = nextConfig

module.exports = {
  images: {
    domains: [
      'images.unsplash.com'
    ]
  }
}



// 1. dodajemo image objekt sa domains nizom
